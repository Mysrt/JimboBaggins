<%= form_for(@jimbo) do |f| %>
  <% if @jimbo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jimbo.errors.count, "error") %> prohibited this jimbo from being saved:</h2>

      <ul>
      <% @jimbo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
